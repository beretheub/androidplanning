<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.berenice.androidplanning">

    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo_raid_noir"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo_raid_noir"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity
            android:name=".sendSms.SendSmsActivity"
            android:label="@string/title_activity_send_sms"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".task.taskActivity"
            android:label="@string/title_activity_task"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".schedule.ScheduleActivity"
            android:label="@string/title_activity_fiche"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".database.TestDbActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".buro.BuroActivity"
            android:theme="@style/AppTheme.NoActionBar"
            android:label="@string/title_activity_buro"/>
        <activity
            android:name=".buro.LoginActivity"
            android:theme="@style/AppTheme.NoActionBar"
            android:label="@string/title_activity_login"/>
    </application>

</manifest>